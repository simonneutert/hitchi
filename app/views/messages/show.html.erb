<div class="row text-center">
  <div class="col-md-6 col-md-offset-3">
    <h2><%= link_to offer_path(@message.offer) do %>Abfahrt:
      <%= @message.offer.departuredate.strftime("%d.%m.%Y").to_s %><br>von
      <%= @message.offer.departurelocal %>
      nach
      <%= @message.offer.destinationlocal %><% end %></h2>
    <p><%= link_to 'Zur Anzeige', offer_path(@message.offer), class: "btn btn-info" %></p>
  </div>
</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="card shadow">
      <div class="card-top">
        <p>
          Angebot
        </p>
      </div>

      <div class="card-body-msg">
        <p><%= @message.content %></p>
      </div>

      <div class="card-footer">
        <p>Abfahrt:
          <strong><%= @message.offer.departuredate.strftime("%d.%m.%Y").to_s %></strong>
          von
          <strong><%= @message.offer.departurelocal %></strong>
          nach
          <strong><%= @message.offer.destinationlocal %></strong>
        </p>
      </div>
    </div>
  </div>
</div>

<% if @answers.any? %>
<% @answers.each do |answer| %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="card card-answer shadow">
      <div class="card-body-msg">
        <p>
          <%= answer.content %></p>
      </div>
      <div class="card-footer">
        <p>
          <small><%= answer.created_at.strftime("%H:%M Uhr - %d.%m.%Y").to_s %></small>
        </p>
      </div>
    </div>
  </div>
</div>
<% end %>
<% end %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <br class="visible-xs">
    <%= nested_form_for(@answer, url: message_answers_path(@message)) do |f| %>

    <div class="field form-group">
      <%= f.label :content, 'Deine Antwort: ' %>
      <small>(140 Zeichen)</small><br>
      <%= f.text_area :content, class: "form-control" %>
    </div>

    <div class="actions">
      <br><%= f.submit 'antworten', class: "btn btn-primary pull-right" %><%= link_to 'zurück', messages_path, class: "btn btn-danger pull-left" %>

    </div>
    <% end %>
  </div>
</div>
<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <hr class="hroffersplit">
    <p class="text-center">
      <strong>Wichtig!</strong>
      hitchi koppelt Nachrichten und Anzeigen. Wird die Anzeige gelöscht, werden auch die dazugehörigen Nachrichten und Antworten gelöscht!</p>
    <p class="text-center">So herrscht hier immer Ordnung und du behältst die Übersicht :)</p>
  </div>
</div>

<!-- <div class="row">
  <div class="col-md-6 col-md-offset-3"><br>
<%= link_to 'Antworten', new_message_answer_path(@message), class: "btn btn-info pull-right" %>
<%= link_to 'zurück', messages_path, class: "btn btn-danger pull-left" %>
</div></div> -->
